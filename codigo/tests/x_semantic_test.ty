fun main() =
    var x: i32 = 10
    var y: i32

    -- 1. Asignación a variable no declarada
    z = 20

    -- 2. Asignación a variable constante
    const C: i32 = 100
    C = 200

    -- 3. Variable no inicializada
    var a: i32
    var b: i32 = a + 5
end

fun scope_test() =
    var local: i32 = 5
    local = 10
end

fun scope_test_error() =
    local = 20 -- Error: local no declarada en este ámbito
end

fun const_param_test(const p: i32) =
    p = 10 -- Error: no se puede asignar a constante
end
